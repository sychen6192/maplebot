mss==9.0.1
numpy>=1.24
opencv-python>=4.8
PyYAML>=6.0

# 系統與遊戲行程監看：偵測遊戲當掉/被關掉（bot 會立刻停手而不是對著桌面按鍵）、
# CPU/記憶體。沒裝的話這一層自動停用，其他功能不受影響
psutil>=5.9

# 開發/測試
pytest>=7.4

# 選配：收工報告的曲線圖（沒裝就只出 .md 與 .json 摘要）
# matplotlib>=3.7

# 選配：YOLO 怪物偵測（RTX 5090 建議路線，見 README「進階：ML 感知層」）
# ultralytics>=8.3

# 選配：只跑 ONNX 推理的機器裝這個就好，不需要 torch（見 tools/export_onnx.py）
# onnxruntime>=1.17
